#include "main.h"
#include <string.h>

/**
 * _strcmp - start of program
 * @s1: parameter
 * @s2: parameter
 * Return: return 0 if strings compared are the same, else something else
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}

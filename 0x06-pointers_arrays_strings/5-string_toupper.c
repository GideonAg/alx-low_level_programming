#include "main."
#include <string.h>

/**
 * string_toupper - start of program
 * Retrun: Always 0
 */

char *string_toupper(char *)
{
	int len = strlen(*);
	int i;

	for (i = 0; i < len; i++)
	{
		if (islow(*[i]))
			toupper(*[i]);
	}
}

#include "main.h"
#include <string.h>

/**
 * _strcat - start of program
 * @dest: parameter
 * @src: parameter
 * Return: a pointer
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}

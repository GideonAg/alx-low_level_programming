#include <string.h>
#include "main.h"
#include <stdio.h>

/**
 * _print_rev_recursion - start
 * @s: parameter
 * Return: Always 0
 */

void _print_rev_recursion(char *s)
{
	int len = strlen(s) - 1;
	int i;

	char *strrev(s);
	puts(s);
}
